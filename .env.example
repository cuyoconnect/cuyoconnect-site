# Redeploy automático (Supabase → /api/trigger-redeploy → Vercel Deploy Hook)
# Generá un secreto largo y el mismo valor en Vercel + header del webhook en Supabase.
# DEPLOY_TRIGGER_SECRET=
# VERCEL_DEPLOY_HOOK_URL=
